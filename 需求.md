# Image SEO Optimizer - 需求文档 (Single Mode + AI)

## 🎯 项目概述
开发一个单图处理桌面应用。用户拖入一张图片，填写关键词，软件自动进行格式转换（WebP）、尺寸调整，并调用 LLM 生成 SEO 元数据。

## 🛠️ 技术栈
- **GUI**: PySide6
- **图像**: Pillow (PIL) + pillow-heif
- **AI/网络**: `requests` 或 `openai` 库 (用于调用兼容 OpenAI 接口的大模型)
- **配置存储**: `QSettings` (保存 API Key 等敏感信息)

## 🎨 界面设计需求

### 总体布局
- **窗口大小**: 默认 1000x700，允许缩放。
- **布局结构**: 左右分栏。左侧 (65%) 为预览区，右侧 (35%) 为控制/信息区。

### 1. 状态一：初始/待机状态
- **左侧预览区**:
  - 显示大大的虚线框图标。
  - 文字: "拖入单张图片 或 点击选择"。
- **右侧控制区**:
  - 顶部: **"AI 设置" 按钮** (齿轮图标)。
  - 中部:
    - **关键词输入框**: 标签 "Target Keyword"。
    - **输出宽度**: 单选按钮组 (1200px [默认], 800px, 600px, Original)。
  - 底部: "开始处理" 按钮 (初始禁用，拖入图片后启用)。

### 2. 状态二：处理完成 (对比与编辑)
- **左侧预览区**:
  - 切换为 **"Before/After 对比组件"**。
  - 中间有拖动杆，左边显示原图，右边显示处理后的 WebP。
  - 顶部显示: `Original: 2.5MB (JPG)` vs `Optimized: 150KB (WebP)`。

- **右侧控制区 (切换为结果编辑模式)**:
  - **文件信息**: 显示新文件名预览 (如 `summer-dress-1.webp`)。
  - **SEO 元数据编辑表单**:
    - **Title**: 输入框 (支持 AI 生成)。
    - **Alt Text**: 多行输入框 (支持 AI 生成)。
    - **按钮**: "🪄 AI Generate" (点击后调用 LLM 重新生成)。
  - **底部操作**:
    - "Save & New" (保存当前并重置界面，准备下一张)。
    - "Open Folder" (打开输出目录)。

## 🧠 AI 智能生成逻辑

### 配置项 (Settings Dialog)
- **API Provider**: 下拉菜单 (OpenAI / DeepSeek / Gemini / Custom)。
- **Base URL**: 输入框 (例如 `https://api.deepseek.com/v1`)。
- **API Key**: 密码输入框 (显示为 `******`)。
- **Model Name**: 输入框 (例如 `deepseek-chat` 或 `gpt-4o`)。
- **Prompt Template**: 允许用户自定义提示词 (提供默认值)。

### 生成逻辑
- **触发时机**: 点击 "开始处理" 后自动触发，或在结果页点击 "AI Generate" 手动触发。
- **输入**: 用户填写的 "Target Keyword" + 原文件名。
- **输出**: JSON 格式，包含 `title` 和 `alt_text`。
- **默认 Prompt**: 
  > "You are an SEO expert. Generate an optimized Product Title and Alt Text for an image based on this keyword: '{keyword}'. Return valid JSON: {"title": "...", "alt_text": "..."}"

## 📂 核心处理逻辑 (Backend)
1. **图片处理**:
   - 转换为 WebP (质量 80)。
   - Resize 到指定宽度 (保持比例)。
   - Strip Metadata (移除原始 EXIF 以减小体积)。
2. **文件重命名**:
   - 逻辑: `keyword.webp`。
   - 冲突检测: 如果存在，自动添加后缀 `-1`, `-2`。